#include<stdio.h>
int main()
{
        int a[100],i,size;
        printf("Enter the Element of array of size :-");
        scanf("%d",&size);
        for(i=0;i<size;i++)
        {
                printf("\nEnter the Element of")
        }
        for(i=0;i<size;i++)
        {
                
        }
}